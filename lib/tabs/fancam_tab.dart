import 'package:flutter/material.dart';

class FanCamTab extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return SingleChildScrollView(
      child: Text('FanCam'),
    );
  }
}